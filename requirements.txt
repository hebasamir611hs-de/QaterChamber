# QA Automation Framework — pinned dependencies
# --- Test runner + reporting (every surface) ---
pytest==8.3.3
pytest-base-url==2.1.0
pytest-xdist==3.6.1
python-dotenv==1.0.1
allure-pytest==2.13.5

# --- Web (Playwright) ---
playwright==1.47.0
pytest-playwright==0.5.2

# --- axe-playwright-python REMOVED (2026-08-18 mentor review): it was added
#     for web/tests/header/test_accessibility.py, a file that does not exist
#     in this tree — a dead dependency pointing at a dead reference. Re-add
#     it (pinned) in the same commit that actually adds the contrast-audit
#     test (PBI 133381 / GLOBAL-ACCESSIBILITY-TC-005), not before. ---
